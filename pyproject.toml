[build-system]
requires = ["setuptools>=68"]
build-backend = "setuptools.build_meta"

[project]
name = "astra-1"
version = "0.1.0"
description = "Next-generation cognitive AI architecture research platform"
requires-python = ">=3.11"

[tool.setuptools.packages.find]
include = ["astra_core*"]
